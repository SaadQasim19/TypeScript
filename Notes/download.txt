check node installed .
check npm version.

Globally : 
npm install -g typescript ( if not work then add sudo in start) and enter

then :
check tsc -v

it will work . 


Locally -- for Project
HERE y means yes to all questions that are asked after npm init..
npm init -y                          # Step 1: Initialize project ( package.json)
npm install --save-dev typescript   # Step 2: Install TS locally
npx tsc --init                      # Step 3: Create tsconfig.json

